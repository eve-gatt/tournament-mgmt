extends ../../layout/default-layout

block styles
    style.

block content
    h2 Overlay mode

    p Coordinate with the ref!
    p As soon as they click 'record' on the match lineup using the Referee page your overlays will be ready to updated.
    p Once this has happened you need to click-on the overlay name below to get it to refresh.
    ul
        li: a(href="#", onclick="javascript:send(1)") OBS Instructions
        li: a(href="#", onclick="javascript:send(2)") "VS" & big team logos
        li: a(href="#", onclick="javascript:send(4)") Cam view overlay

    h3 examples / work in progress
    ul
        li: a(href="#", onclick="javascript:send(3)") e.g. Red Team lineup

block scripts
    script.
        function send(num) {
            d3.json('/auth/stream/manage/' + num, {
                method: 'POST',
                headers: {
                    "Content-type": "application/json; charset=UTF-8"
                },
                body: {}
            }).then(json => {
                console.log('sent');
            });
        }
